spring.application.name=Farmer-calculator

# Force UTF-8 encoding (Spring Boot 3+)
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Ensure Tomcat handles UTF-8 properly
server.tomcat.uri-encoding=UTF-8

server.port=${PORT:8080}  # Use Railway's dynamic port or default to 8080

